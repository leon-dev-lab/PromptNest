# Contributing

感谢参与 PromptNest。

1. Fork 仓库并从 `main` 创建功能分支。
2. 保持修改聚焦，尽量不要提交 `frontend/node_modules`、构建产物或个人 `prompts.json`。
3. 提交前至少运行前端构建和 Go 测试。
4. Pull Request 请说明改动目的、主要行为变化和必要的界面截图。

## 开发

Windows 可直接运行：

```bat
Dev.bat
```

生产构建：

```bat
Build_EXE.bat
```
